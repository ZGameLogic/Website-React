export type SOTDataPoint = {
    id: number;
    ben: boolean;
    patrick: boolean;
    jj: boolean;
    greg: boolean;
    success: boolean;
    proposed: Date;
};

export type SuccessRate = {
    name: string,
    rate: number
}